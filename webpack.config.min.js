var path = require('path');
var webpack = require('webpack');
var config = require('./webpack.config');


config.output = {
    path: __dirname,
    filename: 'dist/osm-[name].min.js',
    libraryTarget: 'umd',
    library: ['angular-osm', 'name']
};
config.plugins = [
    new webpack.optimize.UglifyJsPlugin({
        compress: {
            warnings: false
        }
    })
];
module.exports = config;
